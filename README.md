# Customer_Segmentation
This project performs customer segmentation using the Online Retail dataset by applying RFM analysis (Recency, Frequency, Monetary), followed by K-Means clustering to group customers based on purchasing behavior. It also includes data cleaning, preprocessing, visualization, PCA dimensionality reduction, and cluster interpretation.
